# Git Workflow

After committing changes, always push to the remote immediately using `git push`. Do not wait for the user to ask — push is part of the commit workflow.

If the branch has no upstream yet, use `git push -u origin <branch-name>`.
